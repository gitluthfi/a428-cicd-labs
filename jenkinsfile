pipeline {
    agent {
        docker { image 'node:16.13.1-alphine' }
    }
    stages {
        stage('Build') { 
            steps {
                sh 'npm install' 
            }
        }
    }
}
