pipeline {
    agent {
        docker {
            image 'node:16-buster-slim'
            args '-p 3000:3000'
        }
    }
    stages {
        stage('Build') {
            steps {
                sh 'npm install'
            }
        }
        stage('Manual Approval') {
            steps {
                input "Apakah Anda yakin ingin melanjutkan ke stage deploy?"
                }
            }
        stage('Deploy') {
            when {
                expression { params.APPROVER == 'Proceed' }
            }
            steps {
                sh './jenkins/scripts/deploy.sh' 
            }
        }
    }
}
